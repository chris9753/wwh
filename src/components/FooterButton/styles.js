import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  footerContainer: {
    height: 50,
    width: '92%',
    margin: 5,
    alignSelf: 'center',
    alignItems: 'center',
    justifyContent: 'center',
    borderWidth: 1.5,
    borderRadius: 4,
    // borderColor: DynamicAppStyles["light"].hairlineColor,
    marginBottom: 50,
    flexDirection: 'row',
  },
  footerTitle: {
    fontSize: 17,
    // padding: 10,
    // color: DynamicAppStyles["light"].mainTextColor,
  },
});

export default styles;
